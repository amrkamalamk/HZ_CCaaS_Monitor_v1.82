
'use client';

import App from '../App';

/**
 * Next.js entry point that renders the main App component.
 * This keeps the standalone (index.html) and Next.js versions perfectly in sync.
 */
export default function Home() {
  return <App />;
}
